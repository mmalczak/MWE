This is temporary file, to be deleted

Check available nvidia devices:
> ls -la /dev | grep nvidia


Map all devices into the container:

> docker run -it --device /dev/nvidia0:/dev/nvidia0  --device /dev/nvidia-caps:/dev/nvidia-caps  --device /dev/nvidiactl:/dev/nvidiactl  --device /dev/nvidia-modeset:/dev/nvidia-modeset  --device /dev/nvidia-uvm:/dev/nvidia-uvm  --device /dev/nvidia-uvm-tools:/dev/nvidia-uvm-tools mwe bash

Run CUDA sample (in container):
> /src/mwe/build/cuda_samples/UnifiedMemoryPerf/UnifiedMemoryPerf_sample
